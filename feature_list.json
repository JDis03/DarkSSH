{
  "features": [
    {
      "id": "feat-001",
      "name": "OS Detection Icons in SSH Tabs",
      "description": "Auto-detect Linux distro (Arch, Ubuntu, Debian, etc.) and show icon in tab bar (Termius-style)",
      "dependencies": [],
      "status": "done",
      "evidence": "OsType enum with 20+ distros, OsDetector via shell script, Room migration v3 with osType field, DistroIcons.kt with vector graphics, pulsing animation for UNKNOWN state"
    },
    {
      "id": "feat-002",
      "name": "Terminal Zoom Fix",
      "description": "Fix terminal zoom to adjust cols/rows properly (content stays on screen like Termius)",
      "dependencies": [],
      "status": "done",
      "evidence": "Changed from canvas.scale() to setTextSize() so updateSize() recalculates cols/rows, added onTerminalSizeChanged callback that sends SIGWINCH to SSH for TUI auto-resize"
    },
    {
      "id": "feat-003",
      "name": "WorkManager Background Uploads",
      "description": "SFTP uploads continue in background even when app closes (WorkManager + foreground notification)",
      "dependencies": [],
      "status": "done",
      "evidence": "UploadWorker with ForegroundServiceType dataSync, hybrid approach (WorkManager when connected, direct fallback), progress with startTime for speed calculation, handles app closure and process restart"
    },
    {
      "id": "feat-004",
      "name": "Terminal Race Condition Fix",
      "description": "Fix crash when terminal resizes during rendering (extRow out of bounds)",
      "dependencies": [],
      "status": "done",
      "evidence": "TerminalBuffer.externalToInternalRow() clamps instead of throwing, TerminalRenderer uses screen.getScreenRows() instead of mEmulator.mRows to prevent inconsistency"
    },
    {
      "id": "feat-005",
      "name": "SSH Tab Reconnection",
      "description": "Auto-reconnect SSH tabs after long inactivity, detect silent disconnects",
      "dependencies": [],
      "status": "done",
      "evidence": "ConsoleViewModel.reconnect() passes tabId to preserve tab association, observeBridge tracks wasConnected for silent disconnect detection, removed premature LaunchedEffect that showed reconnect overlay on fresh connections"
    },
    {
      "id": "feat-006",
      "name": "Upload Transfer Optimization",
      "description": "Optimize upload speed with periodic flush, fix cancelation, persist transfer state",
      "dependencies": [],
      "status": "done",
      "evidence": "Flush stdin every 512KB for latency resilience, cooperative cancellation with coroutineContext[Job]?.isActive, activeTransfers in companion object survives ViewModel recreation, Hide/Cancel buttons in dialog"
    },
    {
      "id": "pending-001",
      "name": "Bug: New SSH restarts previous one",
      "description": "Opening a new SSH connection appears to restart or interfere with previous connection",
      "dependencies": [],
      "status": "investigating",
      "evidence": "User reported issue after reconnect fix - needs testing to determine if it's visual (overlay) or actual disconnect"
    },
    {
      "id": "pending-002",
      "name": "Test Dependency Fix",
      "description": "Fix missing conscrypt-openjdk-uber:2.5.3 dependency for unit tests",
      "dependencies": [],
      "status": "done",
      "evidence": "Changed conscrypt version from 2.5.3 to 2.5.2 in libs.versions.toml (2.5.3 does not exist in Maven Central), ./init.sh passes with BUILD SUCCESSFUL"
    }
  ]
}
